{
  "name": "faker-json",
  "version": "1.0.0",
  "description": "This is a project to use faker with json server",
  "main": "index.js",
  "scripts": {
    "start": "npm run generate & npm run server",
    "generate": "node ./index.js > ./database.json",
    "server": "json-server --watch ./database.json"
  },
  "author": "Tapas Adhikary",
  "license": "MIT",
  "dependencies": {
    "faker": "^4.1.0",
    "json-server": "^0.15.1"
  }
}
